<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xforceplus.wapp.modules.job.dao.RecordInvoiceStatisticsDao">
    
    <!-- 保存税率数据-->
    <insert id="saveStatistics" >
    	INSERT  INTO t_dx_record_invoice_statistics WITH(ROWLOCK)
	            (
	             invoice_code,
	             invoice_no,
	             detail_amount,
	             tax_rate,
	             tax_amount,
	             total_amount,
	             create_date)
		VALUES (
		        #{rs.invoiceCode},
		        #{rs.invoiceNo},
		        #{rs.detailAmount},
		        #{rs.taxRate},
		        #{rs.taxAmount},
		        #{rs.totalAmount},
		        getdate());
    </insert>
     
</mapper>